---
# defaults file for ansible-role-dotfiles

# This directory will be created in the user's home directory and dotfiles
# will be cloned into it.
dotfiles_conf_dir: ".ansible-managed-config"

dotfiles_repos: []
# - repo: "https://github.com/ctorgalson/dotfiles.git"
#   user: "molecule"
#   name: "dotfiles"
#   version: "master"
#   owner: "molecule"
#   group: "molecule"
#   whitelist:
#     - ".gitconfig"
#     - ".vimrc"

# Set to `true` to force overwriting existing files or symlinks.
dotfiles_force_links: false
